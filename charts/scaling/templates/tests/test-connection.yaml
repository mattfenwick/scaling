apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scaling.fullname" . }}-test-connection"
  labels:
    {{- include "scaling.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scaling.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
